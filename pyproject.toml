[project]
name = "zolib"
version = "0.1.0"
description = "A patch librarian for the Empress ZOIA"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "rich>=14.1.0",
    "typer-slim>=0.19.2",
]
scripts = ["zolib"]
